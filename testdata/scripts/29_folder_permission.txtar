# get folder permission returns the ACL entries for a folder. The owner's
# entry must be present and identifiable by aco_foreign_key == folder id.

pb create folder --name test-perm-folder --json
cp stdout create.json
jsonget create.json id ID
defer pb delete folder --id $ID

pb get folder permission --id $ID --json
stdout aco_foreign_key
stdout $ID
